---
name: Build & Release

on:
  push:
    branches:
      - main
      - release/**
  pull_request:
    branches:
      - main
      - release/**

jobs:
  release:
    permissions:
      contents: write
    uses: martoc/workflow-github-actions/.github/workflows/action.yml@v0
    secrets: inherit
